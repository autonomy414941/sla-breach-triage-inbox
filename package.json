{
  "name": "sla-breach-triage-inbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SLA breach triage workspace with LLM ticket routing and recurring escalation workflow",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
